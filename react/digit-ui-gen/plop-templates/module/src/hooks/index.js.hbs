import { use{{properCase serviceName}}View } from "./use{{properCase serviceName}}View";
import useEstimateDetailsScreen from "./useEstimateDetailsScreen";
import utils from "../utils";

const sample = {
  use{{properCase serviceName}}View
};
const estimates = {
  useEstimateDetailsScreen
};

const Hooks = {
  sample,
  estimates
};

const Utils = {
  browser: {
    sample: () => {},
  },
  sample: {
    ...utils,
  },
};

export const CustomisedHooks = {
  Hooks,
  Utils,
};