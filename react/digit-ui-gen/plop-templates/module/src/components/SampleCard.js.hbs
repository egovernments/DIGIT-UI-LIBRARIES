import { HRIcon, EmployeeModuleCard, AttendanceIcon, PropertyHouse } from "@egovernments/digit-ui-react-components";
import React from "react";
import { useTranslation } from "react-i18next";

const {{properCase moduleName}}Card = () => {
 
  const { t } = useTranslation();

  const propsForModuleCard = {
    Icon: <PropertyHouse />,
    moduleName: t("{{properCase moduleName}}"),
    kpis: [

    ],
    links: [
   
      {
        label: t("{{properCase moduleName}}  Search"),
        link: `/${window?.contextPath}/employee/{{kebabCase moduleName}}/{{kebabCase moduleName}}-individual`,

      },
      {
        label: t("{{properCase moduleName}}  Create"),
        link: `/${window?.contextPath}/employee/{{kebabCase moduleName}}/{{kebabCase moduleName}}-individual`,

      },
    
      {
        label: t("{{properCase moduleName}} Response"),
        link: `/${window?.contextPath}/employee/{{kebabCase moduleName}}/{{kebabCase moduleName}}-response`,

      },
      
    ],
  };

  return <EmployeeModuleCard {...propsForModuleCard} />;
};

export default {{properCase moduleName}}Card;